$(function(){
  let sendMailModal = $('#sendMailModal');

  $('#recipient-text').val('<%= @admin.email %>')
  $('.modal-title').text("<%= I18n.t('labels.send_message_to', name: @admin.name) %>")

  sendMailModal.modal();
  debugger;
  sendMailModal.on('shown.bs.modal', function () {
    $('#subject-tex').focus()
  })
});
