# Projeto loja virtual

# Dependencias do projeto

* ImageMagick

```
 sudo apt-get update
 sudo apt-get install imagemagick
```

* Graphviz - apenas para ambiente de desenvolvimento

```
 sudo apt-get update
 sudo apt-get install graphviz
```